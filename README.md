# Rust Hot Module Replacement Minimal Project

This is an example of how to do hot reloading in rust using Egui.  This example will only work on Linux, but the same technique can be ported to Windows pretty easily.  Run the project with `cargo r`, then edit `src/lib.rs` and see your changes appear on screen automatically within ~1s.
